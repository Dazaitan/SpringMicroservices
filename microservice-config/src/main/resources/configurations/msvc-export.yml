spring:
  application:
    name: msvc-export
  cloud:
    config:
      enabled: false
logging:
  level:
    org:
      springframework:
        cloud:
          config: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always